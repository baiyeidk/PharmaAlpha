[pytest]
# Make `agents/` itself importable so tests can do `from base.xxx import ...`
# without each test file having to inject sys.path manually.
# This matches the runtime behavior (PYTHONPATH=/app/agents in Dockerfile).
pythonpath = .
testpaths = tests
